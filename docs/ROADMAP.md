# Cadoryx 实施路线与验收

## 1. 当前实施状态（2026-09-13）

2026-09-11 完成设计文档；2026-09-12 用户已授权构建整体代码架构、按路线实施并补充路线。以下状态持续更新，只有经过对应验收才标记完成。

| 阶段 | 已有代码和证据 | 当前状态 / 剩余 |
|---|---|---|
| M0 | 项目分层、领域验证、资产租约、事务、历史、多文档路由、真实树/属性；自动化与桌面冒烟 | 基础闭环完成 |
| M1 | 初始 preview.26 NuGet，H2-B2 升级为本地 8.0.1-preview.28.cadoryx.h2b2.2；真实 Viewer、XDE 导入、单位/旋转/源颜色和浮动重建 | M1-Q 本机项完成；仍需完整元数据、外部厂商样本、真实混合 DPI/RDP/长期验证 |
| M2 | 基础建模、预览/确认/取消、属性、精确撤销重做；目标零件、文档内图层/材料、局部实例位置 UI | 基础闭环及 M2-I 完成；共享子装配使独立、外部材料库等仍在后续阶段 |
| M3 | 八节 MessagePack、资产格式目录、JSON v1/MessagePack v2/M3-V/M4-S1/S2 迁移；hash/限额、保存点、设置读取；三格式导出与进程终止恢复 | M3-R、M3-V 已完成，M4 扩展草图/拓扑/历史节；整体仍需更多文件系统故障覆盖 |
| M4 | 草图编辑与关联重算；Box 面/边语义引用；MetroWindow 局部建模；局部／布尔历史与有界多步诊断 | M4-S1/S2、T1/T2、H1/H2-A、H2-B1/B2/B3 声明范围完成；H2-C 用户诊断与持久引用、连续局部建模及曲线区域继续分项实施 |
| M5 | 定义/实例模型和刚体位姿命令 | 未完成：完整装配编辑、约束求解与外部引用 |
| M6 | 资产复用、历史条目上限、容量限制；共享实例/25 MB BRep/128 MiB 载荷读写基准 | 未完成：真实大模型导入/绘制、内存预算、多视口及发布专项验证 |

新增实施要求：

- IO-01：结构节使用 MessagePack 3.1.8，稳定数值 Key、白名单 DTO、UntrustedData 安全选项；禁止 Typeless。JSON 仅作清单及 v1 结构节兼容读取。
- IO-02：每节同时记录 schemaVersion 与 encoding；当前 features v5、document v5、structure v3、presentation/sketches/history v2、geometry/topology v1 均为 MessagePack，旧 JSON v1、MessagePack v2、M3-V、M4-S1/S2/T1/T2 及 history v1 文件经显式规则迁移。字段编号不得重排或复用。
- IO-03：资产目录包含媒体类型、编码/格式版本、内核和写出库来源；旧版本缺失的生产者信息保持未知，不能补成当前版本。跨节迁移必须完整检查输入/输出、容量、取消和引用一致性。
- EX-01：导出入口支持 STEP/STP、IGES/IGS、STL；STL 提供线性/角度网格偏差、二进制/ASCII 和可见对象选项。
- EX-02：格式能力与信息损失必须反馈给用户；导出不改变 `.cadoryx` 保存点；失败或取消保留旧目标文件。
- QA-01：必须包含实际 NuGet 消费测试、序列化往返、布尔/撤销、异步过期、保存点和三格式导出测试。
- QA-02：UI 通过后再标记 M0/M1/M2 完成；不把仅编译的项目或未跑的验收记为完成。
- RC-01：恢复按 RunId / SessionId 隔离，30 秒定时捕获已提交脏状态并持有资产租约；不会改变原文件或保存点。
- RC-02：保留最近两代完整快照，元数据最后发布；来源恢复必须先建立新的持久副本，再清理旧记录。正常关闭清理需等待在途写入。
- QA-03：恢复验收必须终止实际测试进程并重启，通过恢复窗口核对稳定身份、精确资产、未保存状态、原文件校验值及清理。测试使用隔离恢复目录。
- ED-01：建模目标 PartId、LayerId、MaterialId 必须贯穿默认值、预览、确认和持久化；不能在多零件文档中隐式选择第一个零件。
- ED-02：资源删除检查实体和保留特征输出引用；锁定图层约束创建、属性、布尔和重算。实例移动按完整路径解析，共享父级不能假装只修改一个展开路径。
- UI-01：独立应用弹窗使用 `mah:MetroWindow` 和共用风格，保留 owner、居中、取消及关闭行为；不将嵌入式 DialogHost 内容误改为窗口。
- QA-04：交换测试使用固定输入和解析几何预期；同一适配器生成的往返不能替代跨单位源样本。合成源与外部 CAD 文件的证据分开记录。
- QA-05：浮动窗口回归覆盖独立 HwndSource、相机、实例选择、捕获丢失及关闭释放；单机 96 DPI 和短时循环不得等同混合 DPI/RDP/长时间验收。
- EX-03：导入的源面样式默认保留到显示和原生文件；整体覆盖、属性修改和预览必须有明确优先级。STEP/IGES 当前不输出面样式，IGES 曲面输出的实体语义损失必须报告。
- SK-01：草图实体和约束使用稳定 ID；求解在隔离候选上运行，仅成功结果可提交，失败/取消/过期保持文档与历史。Undo/Redo 和文件读取不重新求解。
- SK-02：自由度与冗余是成功解附近的数值诊断；只对复核过的全仿射系统确认冲突。非线性未收敛不能声称无解；冻结轮廓与关联草图必须明确区分。

### 已完成的设计基线

2026-09-11 的设计基线位于 `Cadoryx/docs`，2026-09-12 新增 IMPLEMENTATION.md 并同步实施状态：

| 文件 | 交付 |
|---|---|
| README.md | 阅读入口与当前范围 |
| ARCHITECTURE.md | 分层、项目依赖、布局、会话、线程与所有权 |
| DATA_MODEL.md | ID、版本、零件/装配、体/资产、特征、草图、拓扑引用与约束 |
| COMMANDS_AND_STORAGE.md | 事务、撤销、预览、差量、文件格式、迁移与恢复 |
| OCCT_INTEGRATION.md | 实际源码/包核实、适配器、运行时和 WPF 视口方案 |
| ROADMAP.md | 分阶段实施与验收 |
| RECOVERY.md | 已实施的定时快照、会话恢复协议、磁盘布局、故障边界与验收 |
| RESOURCES_AND_INSTANCES.md | 已实施的建模归属、资源管理、实例位姿、共享边界和 MetroWindow 风格 |
| EXCHANGE_AND_VIEWPORT.md | M1-Q 固定样本、外观与输入修复、真实窗口矩阵、复现和环境边界 |
| FORMAT_EVOLUTION.md | M3-V 资产格式、跨节迁移、旧文件固定样本、桌面升级验证与存储性能 |
| SKETCH_FOUNDATION.md | M4-S1 草图数据、13 种约束、求解/诊断边界、MIT 许可、命令、六节协议与验收 |
| SKETCH_EDITOR.md | M4-S2 草图交互、修订/关联、依赖重算、MetroWindow、文件迁移、使用与验收 |

前六份文档是设计基线，其后的专题文档为实施补充；原来的“本轮仅文档”限制已被用户后续实施授权替代。代码与原草案的已确认差异以本节和后续验证记录为准。

当前证据包含本地 NuGet 的实际消费、几何/存储/导出测试和桌面运行；未覆盖的交付门禁仍保留。版本信息见 [OcctSharp 接入](OCCT_INTEGRATION.md)。

## 2. 实施顺序

采用能够从 UI 操作到文件保存的纵向阶段，每阶段都有实际行为和失败路径。先验证窗口/内核边界，再扩大建模范围。

### M0：领域与会话基础

实施：

- 新建 `Cadoryx.Db`，实现强类型 ID、双精度数值、单位、公差、不可变快照、Part/Assembly/Slot/Body/AssetRef 与领域验证。
- 新建 Kernel.Abstractions；先定义当前阶段确实使用的资产、能力、交换契约，不创建无行为的万能内核接口。
- 新建 Editor 项目，Commands、Session、Selection 初期可放同项目的不同命名空间；长出真实职责后再独立 Commands 程序集。
- 显式文档工厂/作用域；Workspace、CadDocumentViewModel、多文档标签、活动文档命令路由。
- 模型树/属性从文档快照投影；移除示例节点；没有能力时禁用命令并给出恰当原因。

验收：新建两个文档、切换和关闭不串数据；重复定义/循环装配/非法数值被拒绝；相同子装配的两条实例路径正确区分；ViewModels/Db 不引用 OcctSharp 或 WPF 句柄。

### M1：真实内核、导入与视口

实施：

- 创建 Kernel.Occt、Rendering/Rendering.Occt，锁定经过消费验证的 NuGet 版本，配置 win-x64。
- 在现有 WPF View 接入 HwndHost，完成对话框 airspace 处理、独立视图工具行与生命周期。
- STEP/IGES 默认走 XDE，保留受支持的装配、位姿、名称、颜色与单位，生成 Cadoryx ID 和来源映射。
- UI 独立显示上下文；完整模型导航、FitAll、标准视图、实例选择、树与属性同步。
- 文件读取在内核队列准备；活动文档的版本/关闭状态阻止旧结果提交。

验收：已知尺寸/单位的 STEP 与 IGES；嵌套重复装配位置正确；切换文档不丢相机；主窗口对话框可见可操作；Viewer 创建/销毁不泄漏；干净发布目录可运行。

阶段完成时产品是可用的模型查看/检查程序，尚不宣称完整 CAD 建模。

M1-Q 本机项已完成：固定英寸/米 STEP/IGES、两轴嵌套旋转与共享定义、整体和面颜色显示、原生保存、12 次浮动/resize/重新停靠/关闭重开、真实 Capture 丢失及多键/多选回归。真实混合 DPI、RDP、长期资源趋势与外部厂商文件仍待专项；完整面样式导出和 PMI 尚未实现。详见 [交换与视口](EXCHANGE_AND_VIEWPORT.md)。

### M2：首个完整直接编辑闭环

实施：

- Primitive Box/Cylinder、重命名、持久显隐、颜色/材料修改、实例刚体移动。
- Fuse/Cut/Common 和明确的输出数量/空结果策略。
- 工具参数、预览、确认/取消，文档两阶段提交、失败回滚。
- 已提交精确资产与文档状态历史；撤销/重做、保存点和差量场景同步。
- 在 M3 完成前，为阶段演示可采用明确标为开发用途的临时资产仓，不能把未持久化资产假装已保存。

验收：新建 Box → Cylinder → Cut → 撤销 → 重做；确认前不脏；取消无残留；修改共享零件更新全部实例；失败不半提交；撤销/重做不再次运行内核。

M2-I 子阶段已完成：目标零件/图层/材料选择和新建/属性链路、资源管理、父坐标局部位姿、撤销/保存/恢复保真。支持根级实例和父装配只有一个展开实例的嵌套节点；复用父装配的子路径移动明确拒绝，使独立留给 M5。原阶段统一的独立 MetroWindow 内容在当前工作区由 DialogHost 承载，M3-V 已回归；新增独立窗口仍遵循 MetroWindow 约定。详见 [资源与实例](RESOURCES_AND_INSTANCES.md)。

### M3：原生文档与可靠文件操作

实施：

- `.cadoryx` ZIP、manifest、分节 DTO、资产目录、hash 与逐节迁移。
- 只读取设置/摘要；原子保存、另存为、保存点、恢复快照。
- 当前特征图所有可达源资产完整嵌入；不要只存屏幕上可见的最新结果。
- 打开错误/未知版本/缺失能力边界；STEP/IGES/STL 导出和信息损失报告。

验收：关闭再打开后稳定 ID、装配结构、BRep、材料和命名视图正确；破损包和未来必需节不静默加载；保存中断保留旧文件；异步保存时新编辑仍标脏；无内核初始化的 settings 读取。

M2 + M3 是第一份“能建模、能撤销、能保存恢复”的交付基线。不要先把 Ribbon 上的几十个建模按钮全部做成占位，再补这条闭环。

M3-R 恢复子阶段已完成：生产定时器、两代快照、跨进程锁、最新损坏回退、恢复为未保存副本、原路径保护、保存/关闭协调和三语言恢复窗口。已用真实 WPF 进程强制终止与重启验收。M3-V 也已完成：资产格式目录、显式跨编码/跨节迁移、三个固定旧文件、桌面升级闭环及大载荷读写基准。完整 M3 仍需扩展文件系统故障矩阵；不把定时快照称为逐操作无损日志。详细范围见 [RECOVERY.md](RECOVERY.md) 和 [FORMAT_EVOLUTION.md](FORMAT_EVOLUTION.md)。

### M4：草图驱动参数化零件

实施：

- 基准面、草图线/圆/圆弧、捕捉、二维编辑及轮廓合法性。
- Extrude/Revolve 与类型化参数、依赖图、增量重算、抑制/恢复语义。
- 先用数值驱动草图形成可靠轮廓；通用约束求解器单独评估并接入，不能和“有二维曲线”混为一项。
- 定义可支持的拓扑命名范围，接入语义输出与算法历史；圆角/倒角通过该门禁后启用。

验收：修改拉伸距离能重算后继布尔；循环依赖拒绝；失败保留旧状态；拓扑变更后歧义边不自动误选；保存恢复参数和支持范围内的引用；约束求解自由度/冲突诊断与实际几何一致。

M4-S1 已完成有限约束集的基础：点/线/圆与共享点身份、固定平面、13 种约束、解析雅可比与托管 SVD、局部 DOF/冗余/仿射冲突诊断、Upsert/Remove、精确历史、草图节与旧文件迁移、真实进程恢复。冻结多边形已接入 OCCT 拉伸验证。该阶段不包含鼠标草图编辑、圆弧/切线/角度约束或 SketchId 驱动的特征重算；这些按 M4-S2 和后续约束扩展验收。详见 [草图基础](SKETCH_FOUNDATION.md)。

M4-S2 已完成固定零件平面上的点/线/矩形/圆绘制、捕捉、点拖动、尺寸/约束列表、草稿历史、预览/确认/取消，以及模型树和 Ribbon 入口。SketchId/Revision/有序直线 ID 驱动拉伸/旋转及后继闭包重算，失败、锁定、取消或过期时整体保留原状态；引用中的草图不能删除。当前特征区域限单个直线闭环，圆可编辑/约束/保存但不能生成曲线区域，孔洞、圆弧/切线/角度另列扩展。详见 [草图编辑](SKETCH_EDITOR.md)。

### M5：装配编辑与约束

实施：

- 插入、替换、删除实例、使独立、重挂父级、可复用子装配编辑。
- 明确“编辑共享定义”和“仅改当前实例”的上下文。
- 装配约束模型：ConstraintId、参与实例路径、基准/拓扑引用、参数、状态；预留 Fixed/Coincident/Concentric/Distance/Angle 等。
- 先做固定/刚体位置管理；机械配合约束求解独立设计和评估，不视为 OCCT Shape 变换自然附带的功能。
- 外部零件链接与按版本更新独立于嵌入装配；冲突、缺失文件和更新结果可追溯。

验收：重复嵌套子装配选中与编辑正确；重挂父级保世界位置；定义共享影响可解释；不允许环；缺失拓扑的配合明确失效；撤销保持身份和引用完整。

### M6：大型模型、交付与扩展

实施：

- 大型模型的资产/历史预算、实例复用、树虚拟化、按需几何加载和自适应网格显示。
- 基于实际测量决定多内核 worker、进程隔离或视口合成路线。
- 多视口、测量/剖切、更多局部建模、工程标注和命令行逐项纵向交付。
- 可复现打包、版本兼容、恢复、DPI/远程桌面和长时间运行验收。

验收样本和性能预算需要用真实模型确定。建议建三档固定样本：小型精确零件、重复实例装配、大型导入装配；记录文件大小、定义数、实例数、面数、硬件、导入耗时、首帧时间、交互延迟、峰值内存和历史占用，不在未测量时保证“百万面 60fps”。

## 3. 测试组织

| 测试层 | 主要验证 | 环境 |
|---|---|---|
| Db 单元测试 | ID/引用/环、变换组合、单位、公差、不可变快照 | 不需 OCCT/WPF |
| Sketching | 残差/局部秩、冗余与冲突、非线性初值、尺度、取消与预算 | 纯托管 MathNet；独立消费者不加载 OCCT |
| Commands/Editor | 过期结果、失败原子性、保存点、历史分支、预览取消 | 可控 fake kernel + 部分真实内核 |
| IO | 分节迁移、往返、hash、可达资产、未来版本、原子替换失败 | 临时目录，逐版本小样本 |
| Kernel.Occt 集成 | 单位、BRep 合法性、布尔、XDE 装配元数据、所有权 | 对应 NuGet 与 win-x64 原生运行时 |
| Rendering/WPF | 选取映射、DPI、airspace、浮动重建、关闭释放 | Windows 桌面与 OpenGL |
| 发布消费者 | 无参考源码/PATH 的恢复、运行和导入 | 干净目录/目标 Windows 环境 |

几何测试优先断言体积、包围盒、距离、公差内位置与拓扑合法性；不把容易随内核变化的遍历顺序当成稳定功能要求。持久资产 hash 用于字节完整性，不作为两个独立重建结果几何相等的唯一判据。

按变化影响运行检查。文档调整做链接/结构/事实核查；领域变动做领域测试；Viewer 变动必须真实 Windows 验收。编译通过不能替代原生生命周期或视觉验证。

## 4. 待实施时通过试验确定的事项

这些事项不阻塞当前设计，但影响具体性能/功能承诺：

| 事项 | 已定方向 | 需要的证据 |
|---|---|---|
| 消费包版本 | 初始 preview.26；当前固定本地 8.0.1-preview.28.cadoryx.h2b2.2 | 同版本族锁文件、原生 DLL 哈希与实际消费者；公开发布及完整上游发布门禁独立记录 |
| 草图约束求解器 | 已采用独立 ISketchConstraintSolver + MathNet.Numerics 5.0.0（MIT）；解析方程和有上限的托管稠密 SVD 已验收 | 更完整约束集、交互拖动、大草图与奇异构型/分支处理仍需验证 |
| 拓扑持久命名 | 明确版本 + 语义/算法历史，歧义不猜测 | 布尔/圆角变更、上下文保存恢复用例 |
| 实时 WPF 合成视口 | HwndHost 首版 | 若需求确实需要覆盖控件，再评估 D3DImage/帧复制 |
| 大模型 native 上下文缓存 | 独立显示/计算上下文 | 序列化开销、TShape 所有权和并发证据 |
| 大型存储演进 | ZIP 八节、共享修订、池化保存校验；已有 M3-V 五节合成存储基准 | 八节性能、真实导入模型、流式/按需读取和常驻内存预算 |
| 外部装配引用 | 首版嵌入自包含 | 更新策略、版本锁定、缺失引用与路径可迁移性 |

## 5. 验证记录

H2-B3 已完成唯一有界链诊断：32 段上限，图遍历与原生证据预算，逐段所有输入／结果的资产、修订、适配器、指纹、种类及覆盖校验；多路径、拆分／合并、删除和生成关系停止于明确特征，不返回中途目标。新增 38 项测试（15 图结构、23 内核链／生命周期及故障注入），全套 344 项通过，0 失败／跳过。源、链中刀体和末段侧输入重算、精确撤销／重做、保存重开、旧局部适配器接当前布尔链、取消和失效保护均验证。实现与预算见 [多步历史](HISTORY_CHAINS.md)。

H2-B3 最终 `scripts/verify.ps1 -PublishSmoke -WindowSmoke -RecoverySmoke` 通过锁定还原、Release 0 警告／错误及三个桌面模式；日志 `artifacts/h2b3-verify-final.log`，发布目录 `artifacts/publish/20260913-221316`。证据目录为 `artifacts/smoke-20260913-221316`、`artifacts/window-smoke-20260913-221316`、`artifacts/recovery-smoke-20260913-221316`，三个 result.txt 均 PASS，四份绑定日志为空。三段圆角/Cut/Fuse 链及 STEP/IGES/STL 导出通过；生产 30 秒快照后终止／重启恢复两段布尔链为 Resolved，拆分准确返回中间段 Ambiguous，关闭后资产归零。首次沙箱读取用户级 NuGet.Config 失败单独保留在 `artifacts/h2b3-verify.log`，不混作最终通过记录。

本轮保持 .2 本地依赖、0.4.5 写出器和八节 MessagePack 不变，没有新增原生 ABI 或修改 OcctSharp；发布桥 hash 与 B2 最终证据相同。十份固定存储文件原字节与锁文件均无改动；138 个 README/docs 本地链接和 diff 检查通过。没有开放持久跨特征建模引用或自动重绑定，M4-S2 物理鼠标、混合 DPI/RDP／长期资源、大模型及完整上游产品发布门禁未重跑。

H2-B2 最终消费 `8.0.1-preview.28.cadoryx.h2b2.2`：OcctSharp Release/Debug 原生与受影响托管构建通过，真实 BooleanTopologyHistoryTests 各 5/5；C11 固定布局/函数签名通过；两种配置均为 40,570 个导出，相对 Preview.28 仅新增 `occtsharp_boolean_topology_history`、无删除，测试输出各 62 个 DLL 哈希与对应构建完全一致。上游生成文件未修改；本批不包含其完整 Generator/Runtime 套件、干净再生成、完整 release-check、签名或公开发布。源码、契约及范围见 [布尔历史](BOOLEAN_HISTORY.md)。

14 个最终本地 NuGet、原生载荷与 90 处锁文件引用已核对；独立 Modeling/Native 消费者在受限 PATH 下验证 4800 mm³、真实拆分、92 条关系及新原生桥哈希。三个开发工具的锁文件已同步、Release 构建通过。证据为 `artifacts/h2b2-native-audit.json`、`artifacts/h2b2-package-audit.json`、`artifacts/h2b2-consumer-result-final.json` 和 `artifacts/h2b2-tools-build.log`。正式 Preview.28 及先前本地 .1 包保留；没有公开 NuGet 发布。

Cadoryx 最终锁定还原、Release 0 警告/错误、306 测试通过，构建及测试日志 `artifacts/h2b2-verify-v2-final.log`。新增 8 项实际多输入用例，原布尔门禁测试改为真实拆分必须 Ambiguous；H2-B1 合成测试继续独立验证协议。文件写出版本 0.4.5，history v2 与既有八节协议不变，Preview.26 两种旧单输入适配器继续读取及验证。

最终发布产物 `artifacts/publish/20260913-214119` 已验证桥哈希。首次综合桌面检查完成历史/导出后，在局部窗口三语言检查时触及旧 30 秒上限；记录显示持续进展且绑定日志为空，综合上限现扩展为 60 秒。随后复用同一最终产物运行 `Verify-Published.ps1 -IncludeSmoke`，常规、窗口、恢复全部通过，日志 `artifacts/h2b2-published-v2-final.log`；证据分别为 `artifacts/smoke-20260913-214421`、`artifacts/window-smoke-20260913-214421`、`artifacts/recovery-smoke-20260913-214421`。三个 result.txt 均 PASS，四个绑定日志为空。不能将首次超时脚本记为一次完整通过，也不以早期 .1 包的结果代替最终 .2 验证。

布尔冒烟包括第二输入重算、精确撤销/重做、八节重开、92 条关系和 STEP/IGES/STL 导出；生产 30 秒快照后终止/重启恢复完整两侧字段，重新解析分别为 Resolved/Ambiguous/Deleted，关闭后资产归零。已查看恢复后的原生视口截图，中部切开实体的间隙可见。10 份固定存储文件哈希不变，135 个本地 Markdown 链接、两仓库 diff 检查、上游补丁正向幂等及拒绝覆盖用例通过。M4-S2 草图物理鼠标、混合 DPI/RDP、长期资源、真实大模型和完整上游产品发布门禁未重跑。

H2-B1 新增 16 项多输入协议测试，全套 298 项通过、0 失败；日志 `artifacts/h2b-all-final.log`。验证输入参数／源槽位独立、跨输入合并歧义、来源范围／版本／顺序拒绝、同时重排配方和证据仍不能绕过上游依赖、布尔命令原子提交、第二输入重算、无新证据时清空旧映射、精确撤销／重做、MessagePack 往返及冻结 H2-A 迁移。多输入关系采用明确标记的合成证据，生产解析继续拒绝这些地图；不能将协议测试当作原生布尔对应证明。

本轮全解决方案 Release `--no-restore` 构建通过，0 警告／错误，日志 `artifacts/h2b-release.log`；补充测试后的定向构建与 16 项测试通过，日志 `artifacts/h2b-protocol-final.log`。`scripts/verify.ps1 -PublishSmoke -WindowSmoke -RecoverySmoke` 在锁定还原阶段因沙箱无法读取用户级 NuGet.Config 而停止；显式指定仓库 NuGet.Config 后仍受此限制。申请提升权限执行完整验证时，自动审批服务因额度耗尽拒绝执行。因此本轮锁定还原、发布目录、真实窗口与终止／恢复门禁未完成，不以以下 H2-A 历史 PASS 替代。M4-S2 物理鼠标及混合 DPI/RDP、长期资源、大模型门禁未重跑。

H2-A 全套 282 项测试通过（在 H1 的 263 项上新增 19 项，并加强 24 项旋转边操作的历史断言）。新增限定轴舍入和反向拒绝、旋转源连续重算／存储、三组额外可解析放置、一组保守拒绝放置、冻结 H1 适配器兼容，以及真实布尔拆分计数和禁用传播测试。通过不代表 H2-B 的逐源布尔对应或多步传播已完成。

H2-A 最终 `scripts/verify.ps1 -PublishSmoke -WindowSmoke -RecoverySmoke` 通过：锁定还原、Release 0 警告/错误、282 测试；日志 `artifacts/m4t1h2-final.log`，发布目录 `artifacts/publish/20260913-104928`。常规/窗口/恢复证据为 `artifacts/smoke-20260913-104928`、`artifacts/window-smoke-20260913-104928`、`artifacts/recovery-smoke-20260913-104928`，三个 result.txt 均 PASS，四个绑定日志为空。旋转圆角/倒角的 axis-v2 历史重载解析和 STEP/IGES/STL 导出通过；旋转源的生产 30 秒快照后强制终止/重启恢复保留字段并重新解析为 Resolved，关闭资产归零。九份固定文件 hash 核对通过，旧八份不变；103 个本地 Markdown 链接通过。未重跑草图物理鼠标、混合 DPI/RDP、长期资源和大模型性能门禁。

M4-T1-H1 新增 21 项测试（全套 263 项），覆盖实际包逐源关系、两种局部算法、连续三次上游重算、精确撤销/重做、八节往返、旧 T2 迁移、损坏历史、上下文/版本/指纹/取消，以及旋转校验失败时保留合法建模。拆分/合并目前仅有合成关系的拒绝测试，真实布尔与通用传播仍未验收。实现与明确范围见 [算法历史](TOPOLOGY_HISTORY.md)。

M4-T1-H1 最终执行 `scripts/verify.ps1 -PublishSmoke -WindowSmoke -RecoverySmoke` 通过：锁定还原、Release 0 警告/错误、263 测试。完整日志 `artifacts/m4t1h-final.log`；发布目录 `artifacts/publish/20260913-102750`，常规/窗口/恢复证据分别为 `artifacts/smoke-20260913-102750`、`artifacts/window-smoke-20260913-102750`、`artifacts/recovery-smoke-20260913-102750`。三个 result.txt 为 PASS；四个绑定日志为空。

常规 `history-result.json` 记录圆角/倒角各 26 条逐源关系，修改面 Resolved、生成角色 Generated；两种八节文件重开及三格式导出通过。恢复验证生产 30 秒快照、强制终止和重启，精确历史字段保留，重载 BRep 上的历史面重新解析为 Resolved，关闭资产归零。已查看最终中文 MetroWindow 和单独保存的原生倒角视口，计算预览按钮正常；WPF 截图不包含 HwndHost。八份固定存储样本 hash 已核对，原七份字节不变；本轮 96 个本地 Markdown 链接通过。

本轮桌面验收发现局部窗口引用不存在的 LangKeys.Preview，已改用 CalculatePreview；草图窗口同类 UndoDraft/RedoDraft 改用现有 Undo/Redo。全部 XAML 本地化键已核对，局部窗口冒烟增加“已可见”断言，避免把 XAML 构造失败误判成引用失效问题。草图物理鼠标问题本轮未重跑，不能据此宣称通过 M4-S2 鼠标门禁。

M4-T2 新增 42 项测试，全套 242 项通过：十二边 × 圆角/倒角、任意刚体放置、理论体积和 BRep 合法性，上游重算/精确历史/保存重开，显式引用重选、非法参数/原生失败/取消/过期/锁定、不支持输出、旧文件和损坏协议。新增 [局部建模文档](LOCAL_FEATURES.md) 明确当前范围、操作方式、原生选择所有权和 features v5 迁移。

M4-T2 最终脚本 `scripts/verify.ps1 -PublishSmoke -WindowSmoke -RecoverySmoke` 通过：锁定还原、Release 0 警告/错误、242 测试，日志 `artifacts/m4t2-final.log`。发布目录 `artifacts/publish/20260912-230828`；常规/窗口/恢复证据分别为 `artifacts/smoke-20260912-230828`、`artifacts/window-smoke-20260912-230828`、`artifacts/recovery-smoke-20260912-230828`。三个 result.txt 均 PASS，四个绑定日志 0 字节。

新增真实窗口验证包含主窗口命令打开 owner MetroWindow、OCCT 顶面和语义边拾取、高亮、失效引用显式重选、数值绑定、三语言窗口、预览关闭取消与确认；单边倒角实测 5,980 mm³，精确撤销/重做、保存重开和 STEP/IGES/STL 通过。已查看最终中文窗口及对应原生轴测视口截图，倒角面可见；WPF 截图不包含 HwndHost，原生视口另存。输入从投影坐标驱动真实视口适配器，不是物理鼠标或混合 DPI 验收。

恢复测试扩展为真实局部圆角配方及三条拓扑引用，生产 30 秒快照后强制终止/重启，核对全部字段、依赖和精确几何；两条 Resolved、一条 Stale，关闭资产归零。本阶段未重跑 M4-S2 草图物理鼠标输入问题，全新机器、混合 DPI/RDP、长期原生资源和真实大模型门禁继续保留。

M4-T1 当前新增 22 项测试，全套 200 项通过。真实 preview.26 已验证 Box 的六面/十二边、刚体放置、语义重算、精确修订失效、撤销/重做和七节存储；独立重合候选返回 Ambiguous，不做序号回退。冻结 M4-S2 样本加入迁移回归。实现与算法历史限制见 [拓扑引用基础](TOPOLOGY_REFERENCES.md)。下面保留 M4-S2 原始验收记录，不能将旧证据当作新阶段的执行结果。

M4-T1 最终验证：`scripts/verify.ps1 -PublishSmoke -WindowSmoke -RecoverySmoke` 通过，锁定还原、Release 0 警告/错误、200 测试。日志 `artifacts/m4t1-final.log`，发布目录 `artifacts/publish/20260912-224513`；常规/窗口/恢复证据分别为 `artifacts/smoke-20260912-224513`、`artifacts/window-smoke-20260912-224513`、`artifacts/recovery-smoke-20260912-224513`。常规和恢复均生成 topology-result.json：语义引用 Resolved、旧精确修订 Stale；生产 30 秒定时快照后终止/重启，稳定引用字段及解析行为保持一致，关闭资产归零。STEP/IGES/STL 导出保持通过。

本轮未通过草图鼠标冒烟：两次完整脚本在 Rectangle pointer drawing 处停止；补充诊断确认 Win32 光标定位正常，但 WPF 的画布坐标不正确且没有命中对象。已保留 `artifacts/sketch-input-diagnostic-detail/pointer-failure.png` 和 result.txt，并增加光标返回值/范围/路由检查。最终输入诊断见 `artifacts/sketch-input-final-20260912-224513/result.txt`。因此不宣称本轮 `-SketchSmoke` 通过，也不将历史 M4-S2 的通过结果替代本轮鼠标验收；草图手动输入仍需在可正常路由输入的桌面会话复核。T1 未改草图产品输入实现。

- .NET SDK 固定为 10.0.401，目标 net10.0 / net10.0-windows，native x64。
- Release 全解决方案构建通过，0 警告、0 错误。
- 锁定依赖还原通过；`Cadoryx.Tests`：178 项通过，0 失败、0 跳过。此前 157 项均保留；M4-S2 新增 21 项：6 项关联/迁移、8 项草稿/编辑会话、7 项损坏协议，覆盖关联拉伸/旋转、多个根与共享后继、失败/锁定保护、原子建零件、数值输入、过期/取消、引用缓存和旧文件。
- WPF 真实冒烟通过：预览/提交、树、STEP 导入、标签切换、撤销重做、保存、三格式导出；菜单命令打开资源内容并输入名称/密度，验证创建归属、位姿、图层显隐和三语言布局。当前内容由 DialogHost Popup 承载；测试改用 DialogSession.Content 和可见状态，修复主窗口视觉树漏查造成的失败。原有独立窗口采用 MetroWindow 的约定继续保留。
- 独立 publish 目录冒烟通过：进程工作目录为发布目录，PATH 仅保留 Windows 和 .NET，移除 OCCT/CSF/CASROOT 环境配置；仍使用本机已安装 .NET Desktop Runtime，不能等同于全新机器验收。
- 构建/验证脚本：`scripts/verify.ps1`；加 `-PublishSmoke` 验证发布目录，`-WindowSmoke` 验证源颜色、拾取与浮动矩阵，`-RecoverySmoke` 验证生产 30 秒定时器及终止/重启恢复。诊断布局和恢复数据均使用隔离目录。
- 最终完整脚本 `-PublishSmoke -WindowSmoke -SketchSmoke -RecoverySmoke` 通过：发布目录 `artifacts/publish/20260912-182845`；常规、窗口、草图编辑、恢复证据分别为 `artifacts/smoke-20260912-182845`、`artifacts/window-smoke-20260912-182845`、`artifacts/sketch-editor-smoke-20260912-182845`、`artifacts/recovery-smoke-20260912-182845`。四个 result.txt 为 PASS；绑定日志已检查。Native 运行基线、MathNet DLL 和许可证随发布输出。
- 新增真实草图编辑验收：主窗口命令打开 owner MetroWindow，鼠标输入链绘制点/直线/矩形/圆和拖动点；实际控件尺寸绑定、三语言布局、关闭取消和冲突禁用确认。关联拉伸后切除 10×10×10 mm 方块，矩形宽度 40→60 后整体重算到 17,000 mm³；一次精确撤销/重做、保存重开、STEP/IGES/STL 导出和关闭资产归零均通过。已查看最终中文窗口截图；具体观察见草图编辑证据中的 observations.json。
- 新版恢复验收在生产 30 秒定时快照后强制终止并重启，逐项核对草图修订、特征关联/配方/结果、稳定身份、精确资产及原文件 hash，正常关闭清理通过。
- WPF 草图诊断通过组合根/会话求解矩形、宽度 40→60、撤销/重做和冲突失败保护；冻结轮廓生成 60×30×10 mm 拉伸体，实测体积约 18,000 mm³，保存/重开保留草图和精确几何。结果为常规证据中的 `sketch-result.json`，已查看最终 `sketch-extrusion.png`；这不是图形化草图编辑 UI 验收。
- 保留 M4-S1 独立托管消费者历史记录 `artifacts/sketch-probe-20260912-174118.json`：ManagedLinearAlgebraProvider、nativeKernelLoaded=false、remainingAssets=0；8 变量/8 方程、Rank=8、DOF=0、FullyConstrained。本阶段独立读取旧文件的迁移也由固定 M4-S1 文件回归覆盖。
- 真实窗口继续打开/保存/关闭/重开三个固定旧文件，核对当前六节目录、ID/状态/体/精确资产与源颜色，原文件 hash 不变。继续覆盖颜色/选择/捕获和 12 轮浮动停靠，每次关闭后宿主/资产数为 0；本机单显示器、96 DPI、本地会话，资源采样仅为短时观测。
- 保留 M3-V 历史存储基准 `artifacts/storage-bench-20260912-144824`：25,000 共享实例保存/打开中位数 65.9/64.5 ms；25,218,476 字节 BRep 为 101.9/56.4 ms；128 MiB 扩展载荷为 1,726.2/605.3 ms。独立设置读取进程 nativeKernelLoaded=false。本阶段没有重跑六节性能基准；内存与测量边界见 FORMAT_EVOLUTION，不作为当前六节或导入/绘制性能承诺。
- 扩展恢复验收发现并修复关闭顺序问题：停靠布局的 Unloaded 可以延后；现通过文档 Detaching 明确释放原生视图，再释放会话，不依赖等待或 GC。
- 未运行：全新 Windows 环境、混合 DPI/RDP、浮动多窗口长期资源监控、真实大型导入模型的首帧/交互基准、实际磁盘满/断电/系统崩溃矩阵、NuGet 公网发布验证。合成存储基准和受控应用进程强制终止已验收。

## 6. 下一批具体任务

| 顺序 | 任务 | 完成条件 |
|---|---|---|
| 已完成 | M3-V 格式演进 | 资产描述、跨编码/跨节迁移、旧文件固定样本、桌面升级和存储基准已验证 |
| 已完成 | M4-S1 草图模型与求解基础 | 稳定身份、固定平面、13 种约束、托管求解与诊断边界、MIT 许可/部署、命令/历史、六节存储与旧文件迁移、进程恢复已验证 |
| 已完成 | M4-S2 草图编辑闭环 | 固定平面、点/线/矩形/圆绘制及点拖动、尺寸与 13 种约束、候选预览/确认/取消；单直线闭环关联特征及后继原子重算，历史/存储/恢复和迁移已验证 |
| 已完成（限定 Box） | M4-T1 拓扑引用基础 | 六面/十二边语义、显式版本、解析/失效/歧义状态、七节存储、重算与精确撤销；跨算法历史明确 Unsupported |
| 已完成（单条 Box 边） | M4-T2 局部建模交互 | 专用 MetroWindow 面/边拾取、高亮、失效重选、单边等半径圆角/等距倒角预览/确认；依赖重算、历史、存储、导出和恢复 |
| 已完成（校验通过的单次局部算法） | M4-T1-H1 局部历史证据 | 实际包逐源修改/未改变/生成/删除、双端精确资产与版本、八节持久化、重算替换和诊断解析；旋转校验失败不提供映射 |
| 已完成（限定单位轴舍入） | M4-T1-H2-A 旋转历史校验 | 24 项旋转边操作保留映射；旋转重算/存储/恢复、旧 v1 兼容与反向拒绝测试；不支持样本继续无映射 |
| 已完成（Cadoryx 侧协议） | M4-T1-H2-B1 多输入历史接入准备 | 输入参数/源槽位分离、跨输入歧义、配方和依赖顺序核对、BooleanCommand 证据提交、history v2 与旧文件迁移；合成测试不冒充原生证明 |
| 已完成（本地开发包、直接后继） | M4-T1-H2-B2 布尔逐源内核接口 | 同次 Fuse/Cut/Common 的逐源及结果槽位、精确复制对应、真实拆分/共享目标/删除、两侧校验与保存恢复；不包含完整上游产品发布 |
| 已完成（唯一有界链诊断） | M4-T1-H2-B3 多步历史诊断 | 32 段链及图/证据预算、逐段所有输入核对、路径/拆分/合并停止定位、重算/撤销/保存恢复；不开放自动重绑定 |
| 1 | M4-T1-H2-C1 历史诊断交互与查询持久化 | 保存源语义引用及目标 FeatureId，统一 MetroWindow 展示逐段链/停止位置，三语言、异步状态保护、删除/重选/撤销和文件迁移；不保存临时目标索引 |
| 2（独立设计验收） | M4-T1-H2-C2 跨特征引用与显式重选 | 定义消费者上下文/失效策略，精确身份校验后显式确认重选，重算/历史/迁移/恢复闭环；自动重绑定及生成面建模分别验收 |
| 局部建模扩展 | M4-T3 连续编辑 | 已有局部特征参数编辑 UI、多边操作、生成面/边重选、变半径和双距离倒角；分别验证，不并入 T2 已完成范围 |
| 后续扩展 | M4-S3 曲线区域与约束 | 圆/圆弧和孔洞区域、切线/角度、构造几何编辑、更多拖动与捕捉，分项验收 |
| 后续 | M5/M6 装配与规模 | 装配编辑、共享子装配使独立、外部引用、资产预算和真实模型性能，逐项验收 |
| 专项门禁 | M1-Q 环境及互操作余项 | 外部 CAD 厂商文件、真实混合 DPI/RDP、长期资源矩阵；与完整面样式/PMI 导出分项验收 |

下一阶段为 M4-T1-H2-C1：将已验证的 [多步历史诊断](HISTORY_CHAINS.md) 提供为用户可操作的统一风格窗口，并持久化“源语义引用 + 目标特征”的查询定义。重开时重新解析当前文档，展示路径、停止位置及原因；保留失效对象供显式重选，验证三语言、异步旧状态拒绝、精确撤销和新节迁移。诊断查询不充当建模引用。H2-C2 再单独定义可被特征消费的跨特征引用；自动重绑定、通用拓扑命名和生成面继续建模仍需独立验收，H2 整体尚未完成。M4-T3 参数编辑/多边等扩展、M4-S3 曲线区域，以及 M1-Q 环境和 M3 文件系统门禁继续保留。
