<UserControl x:Class="Cadoryx.wpf.Views.Toolboxes.ModelTreeToolboxView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:vm="clr-namespace:Cadoryx.ViewModels.Toolboxes;assembly=Cadoryx.ViewModels">
 <DockPanel Margin="8">
  <TextBlock DockPanel.Dock="Top" Text="勾选实体可多选；点击特征可编辑参数" TextWrapping="Wrap" Margin="0,0,0,8"/>
  <TreeView ItemsSource="{Binding Items}" SelectedItemChanged="OnSelectedItemChanged" BorderThickness="0">
   <TreeView.ItemContainerStyle><Style TargetType="TreeViewItem"><Setter Property="IsExpanded" Value="{Binding IsExpanded,Mode=TwoWay}"/></Style></TreeView.ItemContainerStyle>
   <TreeView.Resources>
    <HierarchicalDataTemplate DataType="{x:Type vm:ModelTreeItemViewModel}" ItemsSource="{Binding Children}">
     <StackPanel Orientation="Horizontal" Margin="0,3">
      <CheckBox IsChecked="{Binding IsChecked}" IsEnabled="{Binding CanCheck}" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBlock Text="{Binding Name}" ToolTip="{Binding Kind}"/>
     </StackPanel>
    </HierarchicalDataTemplate>
   </TreeView.Resources>
  </TreeView>
 </DockPanel>
</UserControl>
